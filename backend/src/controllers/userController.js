// Controllers for users

class UserController {
    getUser(req, res) {
        // logic to get user
    }
    createUser(req, res) {
        // logic to create user
    }
}

module.exports = new UserController();